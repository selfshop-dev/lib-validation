name: Block non-dev PRs to main

on:
  pull_request:
    branches: [ main ]

jobs:
  block:
    uses: selfshop-dev/.github/.github/workflows/block-non-dev-to-main.yml@v1
    permissions:
      contents: read
      pull-requests: read