name: Release Drafter

on:
  workflow_dispatch:
  pull_request:
    types: [ labeled, unlabeled, closed ]
    branches: [ main ]

jobs:
  release:
    uses: selfshop-dev/platform/.github/workflows/release-drafter.yml@v1
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write